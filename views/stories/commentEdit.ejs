<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Comment</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background-color: #f8fafc;
        }
        .form-container {
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.02);
        }
    </style>
</head>

<body class="flex items-center justify-center min-h-screen">
    <div class="form-container bg-white p-8 rounded-lg w-full max-w-md">
        <form action="/review/<%=comments._id%>/story/<%=story._id%>?_method=PUT" method="post">
            <div class="mb-6">
                <h2 class="text-xl font-medium text-gray-800">Edit Comment</h2>
                <p class="text-sm text-gray-500 mt-1">Make your changes below</p>
            </div>
            
            <div class="mb-4">
                <p class="text-sm font-medium text-gray-700 mb-1">Author</p>
                <p class="text-gray-900"><%=comments.author.username%></p>
            </div>
            
            <div class="mb-6">
                <label for="comment" class="block text-sm font-medium text-gray-700 mb-1">Comment</label>
                <textarea 
                    id="comment"
                    name="comment" 
                    class="w-full px-3 py-2 border border-gray-200 rounded-md focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-blue-500 transition"
                    rows="4"><%=comments.comment%></textarea>
            </div>
            
            <div class="flex justify-end space-x-3">
                <button 
                    type="submit"
                    class="px-4 py-2 bg-gray-800 text-white text-sm font-medium rounded-md hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition">
                    Save Changes
                </button>
            </div>
        </form>
    </div>
</body>

</html>