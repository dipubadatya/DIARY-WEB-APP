<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verification Email Sent</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet" />
</head>
<body class="bg-gray-50">
    <div class="min-h-screen flex items-center justify-center p-4">
        <div class="bg-white max-w-md w-full p-8 rounded-lg shadow-md">
            <div class="text-center mb-6">
                <div class="text-blue-500 inline-flex items-center justify-center w-16 h-16 rounded-full bg-blue-50 mb-4">
                    <i class="ri-mail-send-line text-3xl"></i>
                </div>
                <h1 class="text-2xl font-bold text-gray-800">Verify Your Email</h1>
                <p class="text-gray-600 mt-2">We've sent a verification link to <span class="font-semibold"><%= email %></span></p>
            </div>

            <div class="bg-blue-50 border border-blue-100 text-blue-700 p-4 rounded-lg mb-6">
                <div class="flex">
                    <i class="ri-information-line mt-0.5 mr-2"></i>
                    <div>
                        <p class="font-medium">Check your inbox</p>
                        <p class="text-sm mt-1">If you don't see the email, check your spam folder or request a new link below.</p>
                    </div>
                </div>
            </div>

            <div class="text-center">
                <form action="/resend-verification" method="POST" class="mb-4">
                    <input type="hidden" name="email" value="<%= email %>">
                    <button type="submit" class="text-blue-600 hover:text-blue-800 font-medium">
                        Resend Verification Email
                    </button>
                </form>
                <p class="text-gray-600 text-sm">The link will expire in 24 hours.</p>
            </div>

            <% if (success && success.length) { %>
                <div class="mt-4 bg-green-50 text-green-600 px-4 py-2 rounded text-sm">
                    <i class="ri-checkbox-circle-line mr-1"></i> <%= success %>
                </div>
            <% } %>
            
            <% if (error && error.length) { %>
                <div class="mt-4 bg-red-50 text-red-600 px-4 py-2 rounded text-sm">
                    <i class="ri-error-warning-line mr-1"></i> <%= error %>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>