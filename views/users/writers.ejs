<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title><%= writer.username %> | DIARY.</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet" />

    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        accent: '#3b82f6',
                        darkBg: '#0f172a',
                        writerGold: '#503825'
                    },
                    fontFamily: {
                        sans: ['Plus Jakarta Sans', 'Inter', 'sans-serif'],
                        serif: ['Georgia', 'serif']
                    }
                }
            }
        }
    </script>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap');
        body { font-family: 'Plus Jakarta Sans', sans-serif; -webkit-font-smoothing: antialiased; }
        
        /* Premium Background Gradient */
        .writer-hero {
            background: linear-gradient(to bottom, #503825, #18181b);
        }

        .thought-card {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .thought-card:hover {
            background: rgba(0, 0, 0, 0.03);
            transform: translateX(8px);
        }
        .dark .thought-card:hover {
            background: rgba(255, 255, 255, 0.05);
        }

        .no-scrollbar::-webkit-scrollbar { display: none; }
    </style>
</head>

<body class="bg-white dark:bg-darkBg text-slate-900 dark:text-slate-100 transition-colors duration-300">

    <main class="min-h-screen">
        <header class="writer-hero relative text-white pt-20 pb-16 px-6 md:px-12 lg:px-20 overflow-hidden">
            <div class="absolute top-0 right-0 w-96 h-96 bg-white/5 rounded-full blur-3xl -mr-20 -mt-20"></div>
            
            <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center md:items-end gap-10 relative z-10">
                <div class="w-52 h-52 md:w-64 md:h-64 flex-shrink-0 group">
                    <div class="w-full h-full rounded-full p-2 bg-white/10 backdrop-blur-sm shadow-2xl">
                        <img class="w-full h-full rounded-full object-cover shadow-2xl ring-4 ring-white/20 group-hover:scale-[1.02] transition-transform duration-500" 
                             src="<%=writer.image.url%>" 
                             alt="<%=writer.username%>" />
                    </div>
                </div>

                <div class="flex-grow text-center md:text-left">
                    <div class="inline-flex items-center gap-2 px-3 py-1 bg-blue-500/20 backdrop-blur-md border border-blue-400/30 rounded-full mb-4">
                        <i class="ri-verified-badge-fill text-blue-400"></i>
                        <span class="text-[10px] font-black uppercase tracking-widest text-blue-100">Top 5 Storyteller</span>
                    </div>
                    
                    <h1 class="text-5xl sm:text-7xl md:text-8xl font-black mb-4 tracking-tighter leading-none uppercase">
                        <%=writer.username %>
                    </h1>

                    <div class="flex flex-wrap items-center justify-center md:justify-start gap-6 text-sm font-bold opacity-80 uppercase tracking-wider">
                        <div class="flex items-center gap-2">
                            <i class="ri-eye-line text-blue-400 text-lg"></i>
                            <span><%=userStats.totalViews %> Views</span>
                        </div>
                        <div class="flex items-center gap-2">
                            <i class="ri-book-open-line text-blue-400 text-lg"></i>
                            <span><%=writer.stories.length%> Stories</span>
                        </div>
                    </div>
                </div>
            </div>
        </header>

        <div class="max-w-7xl mx-auto px-6 md:px-12 py-8">
            <div class="flex items-center gap-4 border-b border-slate-100 dark:border-slate-800 pb-8">
                <a href="/profile/<%=writer._id%>" class="flex-1 md:flex-none">
                    <button class="w-full px-8 py-3 bg-slate-900 dark:bg-white text-white dark:text-slate-900 rounded-full font-bold text-xs uppercase tracking-widest shadow-xl hover:scale-105 transition-all">
                        Full Profile
                    </button>
                </a>
                <a href="/chat/<%=writer._id%>" class="flex-1 md:flex-none">
                    <button class="w-full px-8 py-3 bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-full font-bold text-xs uppercase tracking-widest hover:bg-slate-50 transition-all flex items-center justify-center gap-2">
                        <i class="ri-chat-smile-3-line"></i> Message
                    </button>
                </a>
            </div>
        </div>

        <div class="max-w-7xl mx-auto px-6 md:px-12 py-10">
            <div class="grid grid-cols-1 lg:grid-cols-12 gap-16">
                
                <div class="lg:col-span-8">
                    <div class="flex items-center justify-between mb-10">
                        <h2 class="text-2xl font-black uppercase tracking-tighter">Thoughts</h2>
                        <div class="h-px flex-grow bg-slate-100 dark:bg-slate-800 ml-8"></div>
                    </div>

                    <div class="space-y-2">
                        <% for (const stories of writer.stories) {%>
                            <a href="/stories/<%=stories._id%>" class="block group">
                                <div class="thought-card flex items-center gap-6 p-4 rounded-3xl">
                                    <div class="relative w-16 h-16 flex-shrink-0">
                                        <img class="w-full h-full rounded-2xl object-cover shadow-md group-hover:scale-110 transition-transform" 
                                             src="<%=stories.image.url%>" 
                                             alt="" />
                                    </div>

                                    <div class="flex-grow min-w-0">
                                        <h3 class="font-bold text-lg leading-tight truncate group-hover:text-accent transition-colors">
                                            <%=stories.title%>
                                        </h3>
                                        <div class="flex items-center gap-4 mt-1 text-[10px] font-black uppercase text-slate-400 tracking-widest">
                                            <span class="flex items-center gap-1"><i class="ri-eye-line text-blue-500"></i> <%=stories.views.length %> Reads</span>
                                            <span>• Insight</span>
                                        </div>
                                    </div>

                                    <div class="w-10 h-10 rounded-full bg-slate-50 dark:bg-slate-800 flex items-center justify-center text-slate-400 group-hover:bg-accent group-hover:text-white transition-all">
                                        <i class="ri-arrow-right-up-line text-xl"></i>
                                    </div>
                                </div>
                            </a>
                        <% } %>
                    </div>
                </div>

                <aside class="lg:col-span-4 hidden lg:block">
                    <div class="sticky top-28 p-8 rounded-[2.5rem] bg-slate-50 dark:bg-white/5 border border-slate-100 dark:border-white/5">
                        <h3 class="text-xs font-black uppercase tracking-widest opacity-40 mb-6 text-center">Engagement Level</h3>
                        <div class="flex flex-col items-center">
                            <div class="w-32 h-32 rounded-full border-8 border-accent/10 flex items-center justify-center mb-6">
                                <span class="text-4xl font-black text-accent"><%=userStats.totalViews > 1000 ? (userStats.totalViews/1000).toFixed(1) + 'K' : userStats.totalViews %></span>
                            </div>
                            <p class="text-sm text-center text-slate-500 font-medium leading-relaxed italic">
                                "This writer consistently provides honest human experiences through DIARY."
                            </p>
                        </div>
                    </div>
                </aside>

            </div>
        </div>
    </main>

    <footer class="py-20 text-center opacity-20 text-xs font-bold uppercase tracking-[0.4em]">
        Diary Writer Profile
    </footer>

</body>

</html>